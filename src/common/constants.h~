#ifndef CONSTANTS_H
#define CONSTANTS_H

#define CONNECTIVITY 4
unsigned long MAXGREYVAL;
#define BOTTOM (-1)
#define MAXDIM 2

extern long memory;
extern long maxmemory;

enum {
  ADDED  = 1 << 0,
  CHANGED= 1 << 1
};



#endif
